# Передача от Димы — ARPU Compass

## Что опубликовано и интегрировано

- 9f437a1: отдельные подтверждающие пары пилотов платных каналов, независимая проверка плана.
- 678eb7b: проверенный атомарный экспорт с provenance/validation, OpenAI-режим сохранён.
- 92872b5: выбор сильнейшего канала разведки в пределах15% бюджета, CSV обновлён.
- 593667d Азима: веб/README интегрированы без конфликта. Его candidate_model.py, analysis/, web/, README и заметки не изменяю.
- Текущий checkpoint: scripts/package_submission.py и release-документация; точныйSHA в git log -1.

## Поставка

Официальная инструкция перечисляет agent.py + submission.csv + requirements.txt. python -X utf8 scripts/package_submission.py после pwsh scripts/verify.ps1 -Release создаёт output/submission/agent.py со встроенными собственными candidate/advisor модулями и архив output/submission.zip. Полная стратегия сохраняется, helper-файлы при upload не нужны. CSV/requirements копируются побайтно, manifest справочный. Проверяются синтаксис/CSV/секреты до записи, входы читаются один раз, запись атомарная. Организаторские данные нужны для исполнения, в uploadZIP не входят. Генерациясама не освежаетCSV: обязательный шаг verify -Release.

Изолированная папка безcandidate_model.py/llm_advisor.py, Python-I: официальный evaluator PASS+685149.52, официальныйCSV совпал побайтно. SHA256 E52D7A2570211B0B52060181AC56DFF5E9CB719A199AA49E344F670F4CA15F40. Исходники скрытой среды не исследовались.

## Совместные результаты

Seed42:20 пилотов/3 кампании,5606 контактов, расходы46598; остаток53402/9394. Девять новых40,41,43..49:9/9 положительных, медиана657090 против предыдущей582083; минимум515723 против505527. Вся40..49 содержит известный42, отдельно помечена. Полный журнал BASELINE; mock не является прогнозом судейского результата.

Веб593667d открыт в браузере Codex на localhost:8080, импортирован свежийoutput/report.json:685150/3/20, после пилотов71200/11400, после плана53402/9394. Console errors пусто. Адаптивные размеры и остальные состояния проверены Азимом в его HANDOFF, здесь не приписываю повторный прогон себе.

## Азиму: независимые следующие действия

1. Pull main, прочитать RUNBOOK — раздел standalone. В README/демо обновить текущие показатели и сдачу: для отдельных upload использовать output/submission/agent.py, а не обрезанный набор модульных файлов. Не коммититьoutputZIP/отчёты.
2. Импортировать новыйreport и обновить свои визуальные материалы. Новые необязательныеscout_channel/provenance/validation/channel_check допускаются интерфейсом1.0; менять его не требуется.
3. Подготовить двухминутный демо-сценарий и короткое описание различий фактического evaluator/прогнозаallocation. Дима оставляет за собойcore/scripts/CSV/release.

Сверкаmain/HANDOFF и проверенныеcheckpoint примерно каждые4 активные минуты. Во времяbenchmark не менять входы. Общие вопросы про официальныйдедлайн/формуupload и guide/template сеть остаются открытыми; автономная поставка не заблокирована.
