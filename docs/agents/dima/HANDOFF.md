# Передача от Димы — новый продуктовый этап

Пользователь подтвердил: осталось больше2часов. Просит красивый удобный интерфейс, ответыOpenAI и более качественные рекомендации. Полный параллельный план — docs/NEXT_STAGE.md, зафиксированный контракт — docs/API.md. Промпт Азиму уже передан пользователю для немедленного старта.

## Азим начинает сразу

Твоя исключительная зона: web/**, docs/DESIGN.md, docs/DEMO.md, README.md, свои заметки/STATUS. Сначала новый визуальный каркас и обзор на реальномJSON; затем панель агента и frontend APIadapter; затем проверки375/768/1440 и демо. Не жди сервера для дизайна/viewer/адаптивности. Сохрани доступность и импорт1.0. Твои candidate_model.py/analysis/ пока замораживаем для стабильных сравнений ядра.

## Дима параллельно

agent.py, llm_advisor.py, новые server.py/report_assistant.py, scripts/, API.md и интеграция. Backend запускает фиксированную команду расчёта, отдаёт валидный снимок и отвечает черезOpenAI по фактам/ссылкам отчёта. Ключ только на сервере. В этой точке контракт опубликован первым, сервер ещё реализуется — не выдавать его за готовый. Официальная standalone-поставка9f8d1da сохраняется.

## Контракт

Одинorigin localhost:8765, относительные/api URL. GET health/report; POST runs; GET runs/id; POST chat. Все структуры, ограничения и заголовокX-ARPU-Token в API.md. report1.0 сохраняется, imported browser report не получает serverreport_id для чата. БезAPI viewer продолжает работать и честно сообщает о недоступном запуске. Никаких ключей/фиктивных KPI/поддельного прогресса воfrontend.

## Следующие три шага

1. Дима публикует контракты/план; Азим сразу делает композицию и первыйUIcheckpoint.
2. Дима реализует и проверяет API/run/chat; Азим подключает frontendadapter поAPI.md.
3. Совместная интеграция, сравнение качества с92872b5, визуальная проверка и новыйrelease.

Примерно каждые4 активные минуты fetch→чужойHANDOFF→проверенныйcheckpoint; pull после текущей серии, не менять входы во время оценки. Незакоммиченные изменения другого компьютера не видны. Точность оцениваем распределением прибыли/риска, а не выдуманнойaccuracy.
