# Передача от Димы — ARPU Compass

## Состояние после интеграции f740180

Модуль кандидатов Азима автоматически подключён, конфликтов нет. Объединённый main: 7dd1fcf после rebase на f740180. Его candidate_model.py, analysis/ и личные заметки Дима не менял. Текущий пакет дополняет release/benchmark и правила синхронизации; точный SHA — git log -1.

Агент: ограниченные initial/feedback OpenAI (ранее оба вызова проверены реально), fallback без ключа, адаптивные повторные пилоты, лимиты/непересекающиеся финальные кампании. Эффект финального канала берётся только из его пилотов. Разведка сейчас начинается с самого дешёвого канала. Candidate API: build_candidates(profile, tariffs, data_dir), ровно3 аргумента.

## Измерения

После кандидатов Азима seed42: PASS +475595, 16 пилотов и3 кампании, 4806 контактов, расходы0. submission.csv пересоздан для совместной версии. Отложенные20..29: текущая версия10/10 положительных, медиана469900; SMS-контроллер e7c9f6f на тех же текущих данных также10/10, медиана603277. Он пока сильнее по прибыли. Дима проверит платные каналы отдельными пилотами после дешёвой разведки. Не подменять эти результаты старыми метриками до candidate_model; полный журнал в BASELINE.

## Азиму для веба

Контракт отчёта1.0 сохранён. resources — после пилотов, planned_resources — после финального плана. allocation — прогноз; evaluation — публичный фактический результат. В pilots необязательны cost, requested_n, observed_lift_total и остатки. Отчёт создаётся python -X utf8 scripts/run_agent.py --offline. Архитектура и дизайн без изменения интерфейсов. Продолжай web/ и README в своей зоне.

## Синхронизация и поставка

По поручению пользователя: примерно каждые4 активные минуты — fetch, посмотреть входящие пути, чужой STATUS/HANDOFF, затем проверенный маленький commit/push. Не перехватывать чужие файлы. Во время benchmark допустим fetch, pull после завершения: входы нельзя менять посреди серии. Незапушенная работа другого компьютера не видна.

verify.ps1/checkpoint.ps1 теперь привязаны к корню независимо от cwd. Перед сдачей pwsh scripts/verify.ps1 -Release: пересоздаёт CSV и проверяет структуру, затем CSV включить в checkpoint. Проверено запуском из scripts/. Benchmark сохраняет resolved SHA, hashes и честные метки источника ресурсных сведений.

## Следующие три шага Димы

1. Отправить этот синхронизированный workflow/CSV checkpoint.
2. Реализовать и сравнить отдельную проверку выгодных платных каналов, не меняя candidate_model/web.
3. Проверить итоговый совместный запуск и провести финальную поставку после веб-checkpoint Азима.

Блокеры: официальный дедлайн/канал сдачи и расхождение guide/template ещё не уточнены; это не мешает текущей локальной работе. Ключи только в окружении. Внутренности среды и скрытые эффекты не читать.
