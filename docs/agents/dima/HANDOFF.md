# Передача от Димы — ARPU Compass

## Текущее состояние

Кейс объявлен: тарифные маркетинговые кампании Beeline. Цель — положительный net ARPU через пилоты и адаптивный выбор кампаний. Полное ТЗ в docs/PROJECT.md, расписание 300 минут в docs/WORK_PLAN.md, интерфейсы в docs/ARCHITECTURE.md. Пакет организаторов скопирован без изменений в корень; runner импортирует agent.py.

Первый каркас опубликован: 1a025b4. Следующий checkpoint содержит выполненную часть Димы: адаптивный Agent, llm_advisor.py, launcher со скрытым вводом, экспорт JSON и обновлённый submission.csv. Offline seed42: PASS, net +21 710.13, 16 пилотов и 2 кампании. Но 3/10 положительных seed и медиана −14 363: устойчивость ещё не достигнута. Оба реальных OpenAI-запроса успешны; онлайн net −18 822, улучшение от LLM не доказано. Подробности в docs/BASELINE.md. Не представлять интеграционный checkpoint как финальную готовность. Точный SHA текущего пакета — git log -1.

## Что подхватить Азиму

Сделать pull main, прочитать PROJECT/WORK_PLAN/ARCHITECTURE и подтвердить своё владение в STATUS. Приоритет — candidate_model.py: build_candidates(profile, tariffs, data_dir) по указанному контракту. Только разрешённые исторические CSV; ID истории и целевой базы не пересекаются. После интеграции — web/ и README. Для UI реальный отчёт создаётся командой python -X utf8 scripts/export_report.py --seed 42; output/report.json локальный и игнорируется Git. Секреты в отчёте отсутствуют.

Дима реализовал llm_advisor.py, оркестрацию/адаптацию и бюджетный контроль в agent.py. Candidate module автоматически импортируется с тремя аргументами (profile, tariffs, data_dir). При отсутствии модуля встроены узкие ценовые гипотезы; перенос исторических медиан оказался неустойчив. Не менять agent.py, llm_advisor.py, scripts/контракты без координации. Личные LOG/HANDOFF/STATUS каждый обновляет сам. Полная инструкция запуска и подключения — docs/RUNBOOK.md.

## Следующие три шага Димы

1. Отправить текущий пакет через checkpoint/push; Азим может начинать data-модуль независимо.
2. После коммита Азима интегрировать candidate_model.py, сопоставить приоры с пилотной обратной связью.
3. Улучшить устойчивость пилотов/распределения и сравнить с первым baseline, не затрагивая web/analysis/ Азима.

## Блокеры и проверки

Официальный дедлайн/канал сдачи ещё не подтверждены. Guide разрешает LLM и 10 минут, template говорит без сети/5 минут: держим автономный режим и консервативный срок. Исходники скрытой среды не читать и не изменять. API-ключ — только окружение. Для bundled Python можно задать ARPU_PYTHON; на Windows запускать с -X utf8. Checkpoint коммитит только явно выбранные файлы или подготовленный индекс и останавливается при ошибке.
