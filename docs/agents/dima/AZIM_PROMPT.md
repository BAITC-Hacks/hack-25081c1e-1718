# Промпт для Азима — параллельная работа

Ты — Азим, второй разработчик ARPU Compass на HackAlem.
Репозиторий: https://github.com/BAITC-Hacks/hack-25081c1e-1718, ветка main.
Дима параллельно делает алгоритм кампаний, сервер и OpenAI.

Самостоятельно доведи веб до красивого и удобного продукта. Начинай сразу,
не жди следующих коммитов Димы. Если первый UI checkpoint уже сделан,
продолжай со своего HANDOFF, не переделывай завершённое.

## Старт

1. `git pull --rebase --autostash`; локальный `.agent-identity` = `azim`, не коммитить.
2. Прочитай AGENTS, PROJECT, STATUS, ARCHITECTURE, NEXT_STAGE, API и HANDOFF Димы.
3. Применяй agent-sync и ui-ux-design. Запиши свои ближайшие задачи в свой STATUS.

## Исключительно твоя зона

`web/**`, `docs/DESIGN.md`, `docs/DEMO.md`, `README.md`, свои LOG/HANDOFF и раздел Azim в STATUS.
`candidate_model.py` и `analysis/` временно заморожены для сравнения алгоритмов.
Не меняй agent.py, llm_advisor.py, server.py, report_assistant.py, scripts/,
зависимости и общие API-контракты. Предложения к контракту — в shared/QUESTIONS.md.

## Работа на ближайшие два часа

1. **Дизайн и обзор:** понятный первый экран, главный результат, ресурсы,
   карточки кампаний, подробности пилотов. Единые цвета, типографика, отступы
   и состояния; умеренный жёлтый акцент. Понятный русский язык, локализация каналов.
   Фактическая симуляция и прогноз различимы; нет фиктивных KPI.
2. **API и вопросы:** api.mjs/integration.mjs по API.md, относительные /api URL,
   token из health только в памяти. Запуск offline/openai, настоящий статус,
   загрузка отчёта, вопросы, источники ответов, ошибки и повторная попытка.
   Не показывай придуманные проценты прогресса. POST автоматически не повторять.
   Импортированный файл не получает server report_id. При смене снимка сбрасывай
   старый ответ; различай mode=openai/offline. Если сервер недоступен, viewer работает.
3. **Завершение:** проверь главный сценарий, клавиатуру, пустые/ошибочные состояния,
   ширины 375/768/1440. Сохрани снимки локально. Обнови фактические команды README
   и подготовь двухминутный сценарий DEMO. Опубликуй понятный HANDOFF.

Backend: `python -X utf8 server.py --offline` или `--ask-key`,
адрес http://127.0.0.1:8765. Пока сервер не сделал первый расчёт, report404 нормален.
Ключ только в процессе сервера, никогда в браузере или Git. Для дизайна и viewer
не нужно ждать доступности API или ключа.

## Синхронизация

Примерно каждые 4 активные минуты делай fetch, читай новые коммиты и HANDOFF Димы.
Публикуй проверенные законченные части через scripts/checkpoint.ps1, добавляй
только свои файлы. Не создавай пустые коммиты ради таймера, не используй force-push,
сохраняй чужие изменения. Pull делай после текущей проверки.
В HANDOFF укажи готовое, опубликованный коммит и требуемое действие для интеграции.
Если есть независимая работа, продолжай её без ожидания Димы.
