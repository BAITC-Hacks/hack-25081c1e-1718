# Передача от Азима — понятные тарифы и суммы

## Текущий checkpoint

После дизайна **b1d4fbe** пользователь попросил объяснить денежные единицы и тарифы для обычного пользователя. Готов `feat: explain tariff offers and monetary units`; точный SHA — `git log -1 -- web/report.mjs` после checkpoint.

- «Какие тарифы предложить»: две стороны «Сейчас → Предложить», ГБ, минуты и точная месячная цена из публичного tariff_dictionary.csv. Номер тарифа сохраняет различимость одинаковых пакетов. RAW коды остаются в отчёте и CSV.
- Вместо у.е. — «ден. ед.» с видимым пояснением «условные денежные единицы». Валюта не задана; тенге/рубли/доллары не приписываются данным.
- Сегменты названы по-русски, поиск понимает «8 ГБ» и «средний доход». Пороговые значения и направления минут доступны в основаниях. 2 000 МБ для LITE не превращены в 2 ГБ. Месячные цены сохраняют дробную часть.
- Несколько текущих тарифов показываются через «или». Тариф №12 содержит один общий пакет 300 минут на других операторов и городские номера. Неизвестным кодам не приписываются параметры. Нулевые пакеты не объявляются недоступностью услуги.
- Каталог только для отображения в существующем web/report.mjs; тест сверяет все 21 записи с исходным CSV. Новых static paths, зависимостей и API полей нет.

## Проверки

- **23/23 Node tests**: прежние 21 + соответствие каталога CSV и представление различимых/общих/неизвестных значений; поиск дополнен понятными названиями.
- Браузер на существующем реальном snapshot сервера8765: 4 ГБ/40 мин/4 678,6 → 8 ГБ/80 мин/5 934,6. Поиск возвращает ожидаемые кампании; каждое из 7 CSV полей сравнено с исходником.
- Локальные UI fixtures: несколько текущих тарифов, неизвестные коды, общий пакет 300 минут. API POST не выполнялись, алгоритм повторно не настраивался.
- Обзор/кампании/пилоты на375/768/1440: без переполнения страницы. Снимки открыты и оценены; JS errors и внешних запросов нет. Контраст пяти разделов и импорт/CSV/ошибочный/пустой/отрицательный отчёт прошли.
- Локальные артефакты: output/readable-tariffs/verification.json и PNG; общий viewer/contrast — output/playwright-v2/viewer-check.json. Официальный verify до commit и после pull выполняет checkpoint.

## Совместимость и прежняя работа

[AFFECTS-OTHERS] Подтянут main до работы, входящих изменений после **6550806/b1d4fbe** нет. Ядро, candidate_model/analysis, server, report_assistant, scripts, исходные CSV и контракты не изменялись. Каталог при изменении публичного CSV надо обновить; тест сообщит о расхождении.

Ранее в b1d4fbe готовы C-монограмма Compass, новый «О результате», лента до20пар с режимами/источниками, клавиатура, ручной retry и защита от старого ответа. История остаётся в памяти страницы, API получает только report_id/message. Прежние15browserfixture-сценариев и реальный offline run→report→2ответа→источник прошли; новый snapshot core6550806 дал +685149.52,20пилотов,3кампании. Live OpenAI проверял Дима.

## Следующие три шага

1. Диме принять этот UI checkpoint и обновить страницу8765. Изменений backend для него не требуется.
2. Диме завершить официальный release/CSV/standalone и финальную интеграцию; при изменениях отчёта Азим исправляет только свою зону.
3. После подтверждения финального релиза и отсутствия необходимых действий приостановить координацию. GitHub сверяется примерно каждые4активные минуты, без пустых коммитов.

Блокеров в зоне Азима нет. Официальная отправка остаётся у команды; скрытые эффекты не исследовались, ключи только в окружении сервера.
