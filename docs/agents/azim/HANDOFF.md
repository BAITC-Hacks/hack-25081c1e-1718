# Azim — RU/KK Tariflow

## Готово

Опубликован UI checkpoint **db56214**; verify до/после pull и push прошли. По прямому запросу пользователя добавлены русский и казахский языки. Переключатель в шапке сохраняет выбор локально. Переведены все пять разделов, тарифы, сегменты, статусы, ошибки и ARIA. Поиск понимает оба языка. Смена языка сохраняет отчёт, черновик, фильтры, раздел и раскрытые карточки. CSV и исходные идентификаторы не меняются. Свободный текст отчёта и уже полученные ответы остаются оригинальными.

Новый web/i18n.mjs согласован через QUESTIONS. Дима добавил статический путь и контракт языка в 161473c. API отправляет optional language только после capabilities.chat_languages; старому серверу — прежний запрос с явной отметкой русского ответа. Язык фиксируется при отправке; ручной повтор использует текущий язык. Response.language необязателен: согласованный язык запроса определяет метку.

## Проверено

33 Node-теста; 6 прежних сценариев чата (запоздалые ответы, повтор, 20 пар, неопределённый запуск); фикстуры KK/старого API; все пять разделов на 375/768/1440 без переполнения. Снимки просмотрены. Реальный сервер 161473c: offline42 → отчёт → KK-вопрос о кампании1 → ответ → собственные пилоты17/18 → фокус на строке17. Внешних вызовов не было; локальный ключ OpenAI не настроен. Provenance.dirty=true из-за работы над UI, это не финальный чистый релиз. Артефакты: output/i18n и output/i18n-real.

Дополнительно: четыре браузерные проверки крайних состояний прошли — старый RU-only сервер при смене языка, сохранение исходных имён/ID, перевод уже показанной ошибки, запрещённый localStorage. Артефакт output/i18n-edges/verification.json. Три финальные формулировки согласованы с жорамал / есептеу нәтижесі. Вкладка8765 обновлена и оставлена на казахском.

## Синхронизация

Приняты 33b8d9e и 161473c. Backend, кандидаты, данные, scripts и зависимости Азим не менял. В QUESTIONS16:38 переданы воспроизведённые ошибки KK-маршрутизации для сынақ/тәуекелдері/байланысу/жорамал, русский остаток перевода и редактура weighted/dedup. Прямой пользовательский запрос двуязычия приоритетнее нового QUALITY_STAGE UI; последний остаётся следующим отдельным этапом.

## Следующие три шага

1. Диме принять UI checkpoint (git log -1 -- web/i18n.mjs), исправить маршрутизацию и редактуру из QUESTIONS16:38.
2. Азиму подтянуть исправление, проверить согласованные KK-вопросы на актуальном сервере и завершить пользовательское двуязычие.
3. Затем интерфейс диагностики по QUALITY_STAGE и совместный финальный релиз. Автоматизацию пока не приостанавливать.
