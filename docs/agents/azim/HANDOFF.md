# Передача от Азима — ARPU Compass

## Первый data checkpoint

Реализован `candidate_model.build_candidates(profile, tariffs, data_dir)` — ровно три аргумента, DataFrame только читаются. Новый коммит: `feat: add cautious public-data candidates` (точный SHA: `git log -1 -- candidate_model.py`). Интеграционная база — e7c9f6f Димы, подтянута перед проверкой. Ядро автоматически загружает модуль, ручное подключение не требуется.

299 кандидатов, 160 сегментов и 22660 абонентов, выражаемых точными current/arpu/data/call фильтрами. До двух целей на сегмент: умеренный переход по цене/пакетам и исторический или пакетный альтернативный вариант. Приоры — только агрегаты пары тарифов и исторического pre-ARPU сегмента: сильное сглаживание к нулю, максимум ±2.5%, LOW/малая поддержка/разный знак оценок дают 0. prior_n — реальные очищенные наблюдения. IDs целевой базы с историей не соединяются. Исторические данные не являются доказательством эффекта кампании.

Обоснования, ограничения и команды — `analysis/README.md`; воспроизводимый аудит — `analysis/audit_public_data.py`; проверки — `analysis/test_candidates.py`. Контракт ARCHITECTURE сохранён. Организаторские файлы, agent.py, llm_advisor.py, scripts/, зависимости и чужие заметки не менялись.

## Проверки

Python 3.12.14 / pandas 3.0.1 / NumPy 2.3.5. `python -X utf8 -m unittest discover -s analysis -v`: 7/7. `python -X utf8 scripts/run_agent.py --offline` на e7c9f6f: candidate_source=candidate_model, warnings=[], 16 пилотов, 3 SMS-кампании, seed42 net **+599050.15**, PASS. После пилотов 88800 у.е. / 12200 контактов; после плана **80776 / 10194**, пилотов осталось 4. Результат одного mock-прогона не является гарантией устойчивости или судейского результата.

## Координация и следующие три шага

Официальная серия seed 0..9 на этой интеграции: **10/10 положительных**, медиана **+587897**, минимум **+429315**, максимум **+608844** (округление официального stdout). Серия фиксирована заранее и не использовалась для настройки чисел модели. Это опубликованный набор проверки, не независимая отложенная валидация. Изменение относительно a78ed41 включает также исправление каналов Димы, поэтому отдельную причинную долю кандидатов не заявляем.

1. Диме: pull main; модуль подхватится автоматически. Пересоздать submission.csv вашим официальным генератором после интеграции; Азим сохраняет границы владения и не коммитит этот общий артефакт. Сравнение контроллеров и отложенная серия остаются у Димы.
2. Азиму: после первого push и следующего pull реализовать web/ по DESIGN — импорт фактического report.json, ресурсы обоих этапов, пилоты, кампании и ограничения. Дополнительные поля пилотов из e7c9f6f учесть как необязательные.
3. Азиму: обновить README, проверить браузер на 375/768/1440, ошибки/пустые данные/клавиатуру; законченный web checkpoint с явным списком и новым handoff.

Main и handoff сверяем примерно каждые 5 минут; коммитим только проверенные законченные части. Вопросы по общим контрактам — через QUESTIONS. Нужды менять контракт сейчас нет. Официальный дедлайн/канал сдачи и расхождение guide/template остаются общими открытыми вопросами. Ключи только в окружении.
