# Tariflow — качество решений, этап 90 минут

Начало: 23.09.2026 15:56 (UTC+5). Пользователь утвердил план улучшения
чистого прироста с контролем риска. Baseline — **полная ревизия4473540**.
Принят новый интерфейс9d748c7; названиеTariflow сохраняем.

## Работа параллельно

- Дима: agent/server/report_assistant, exporter, изолированный benchmark, проверки,
  API/архитектура и финальная поставка. Ограниченные workers помогают только в
  agent.py+его тестах и в новом quality_benchmark+его тестах.
- Азим: web/**, README, DESIGN/DEMO, собственные заметки. Начинай самостоятельно
  с контракта ниже; исходный UI9d748c7 не переделывать.
- candidate_model.py, analysis/**, исходные данные и зависимости заморожены.
- Каждые примерно4активные минуты сверяем GitHub; checkpoint только законченных
  проверенных частей. Входы запущенных сравнений не меняем; benchmarks используют
  отдельные неизменяемые снимки полных ревизий.

## Совместимый контракт report1.0

Все новые поля необязательны. Старый JSON показывает «нет данных» для отсутствующих
диагностик. HTTP/CSV/Agent.act сохраняются. UI не пересчитывает экономику.

### selection_diagnostics

Числа: generated_candidates, tested_candidates (уникальные candidate_id с завершённым
пилотом), tested_variants (candidate_id+channel), confirmed_variants (два завершённых
пилота), selected_variants, unexplored_candidates. Подтверждение само по себе не
означает положительный эффект. reason_counts — количества по причинам ниже.

variants — до20 исследованных вариантов. Каждая запись:
candidate_id, channel, repeats, conservative_net, selected (boolean), reason,
pilot_refs (например pilots.16; UI это пилот17). Нет customerIDs/members.

Причины: selected, mandatory_fallback, insufficient_pilots, nonpositive_estimate,
overlap, budget, contacts, not_selected. Приоритет объяснения отклонения:
недостаточно пилотов → неположительная оценка → пересечение → бюджет → контакты
→ другая причина выбора. Фактический выбранный вариант/mandatory fallback помечаются
перед этими проверками. Отсутствие источников не заменять придуманными пилотами.

### Диагностика allocation

Существующие поля сохраняются. Добавляются template_uncertainty, sample_std,
empirical_se (доли, не проценты), uncertainty_method:
template_floor или max_template_empirical. repeats и n_customers уже существуют;
n_customers — сумма размеров пилотов, не гарантированное число уникальных абонентов.
Отображать «эвристический запас неопределённости», не доверительный интервал.

### forecast_summary и evaluation

forecast_summary: scope="final_campaigns_only", estimated_net, conservative_net,
communication_cost, campaign_count, comparison_to_evaluation="not_comparable".
Это суммы оценок **только финальных кампаний**, без добавления эффектов пилотов.
evaluation.scope="pilots_and_final_deduplicated" — фактический итог всей симуляции.
Разницу этих величин не показывать как ошибку прогноза. Общая подпись метрики:
«Прирост выручки за вычетом расходов на коммуникации».

Новые допустимые ссылки Q&A: selection_diagnostics («Исследование») и
forecast_summary («Прогноз плана»). Азим связывает их с соответствующими блоками;
пока поле отсутствует, ссылка не создаётся. Прежние refs сохраняются.

strategy_config: exploration_policy=baseline|balanced,
uncertainty_mode=template|empirical. Это сведения в технических деталях.

## Азиму — готовые задачи

1. Блок «Что исследовал агент»: создано, исследовано, подтверждено, выбрано;
   разные единицы «гипотезы» и «варианты с каналом» подписать явно.
2. Для кампании — собственные пилоты, число повторов, разброс и осторожная оценка.
3. Показать причины исключения других исследованных вариантов понятным русским языком.
4. Разделить прогноз финального плана и факт всей симуляции. Подключить новые refs.
5. Старые/пустые JSON, отрицательные оценки, неизвестные необязательные поля,
   реальные API/источники,375/768/1440, README/DEMO и свой HANDOFF.

Backend/benchmark не нужны для начала: используй явно тестовые fixtures для новых
необязательных полей; в продуктовом интерфейсе только фактический импорт/сервер.

## Выбор стратегии

Три варианта: новая очередность; эмпирическая неопределённость; оба изменения.
Разработческая серия0–9, затем фиксация одного варианта до просмотра seed100–119.
Кандидатные данные не меняются. Полная старая версия запускается отдельно, без
подмешивания новых helper-модулей. Сохранить хеши кода/данных и результаты каждого seed.

Замена baseline допустима только при: median≥110% baseline; положительная медиана
парных изменений; p10≥baseline; число отрицательных не растёт; ноль нарушений;
каждый прогон≤300с. После holdout параметры не подбирать. Если критерии не выполнены,
оставить baseline-политику и выпустить диагностику/улучшения отчёта.

После фиксации: три online/offline пары120–122, максимум6запросов советника.
Это отдельное исследовательское наблюдение, не критерий замены автономной стратегии.
Последние15минут — CSV, standalone, проверка, пакет, финальный push и handoff.
